/** Used only when location permission is denied or unavailable — keeps the
 *  map/feed usable rather than dead-ending on a blank screen. Tel Aviv, the
 *  current launch market. */
export const FALLBACK_LOCATION = { latitude: 32.0853, longitude: 34.7818 } as const;
